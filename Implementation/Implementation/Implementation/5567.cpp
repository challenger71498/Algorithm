#include <iostream>
#include <stack>
#include <vector>

struct Node {
	Node* child;
	Node* value;
};

struct Tree {
	Node* root;
};

Node* find()

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	int n;
	cin >> n;
	int m;
	cin >> m;

	for (int i = 1; i <= m; ++i) {
		int x, y;
		cin >> x >> y;
		arr[i].insert(x);
		arr[i].insert(y);
	}


}